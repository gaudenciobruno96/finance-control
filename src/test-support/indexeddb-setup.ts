/**
 * Ambiente de IndexedDB em memoria para os testes da Unidade 2.
 *
 * `fake-indexeddb/auto` instala a implementacao nas variaveis globais, de modo
 * que Dexie a encontra sem qualquer alteracao no codigo de producao -- o
 * repositorio nao sabe que esta rodando em teste.
 */

import 'fake-indexeddb/auto'
